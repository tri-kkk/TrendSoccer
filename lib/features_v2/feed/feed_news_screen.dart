import 'package:flutter/material.dart';

class FeedNewsScreen extends StatelessWidget {
  const FeedNewsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('FeedNewsScreen')));
}
